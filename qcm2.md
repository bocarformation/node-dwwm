### Question 1
Quelle commande permet d'installer Express dans un projet Node.js ?

A) `npm install express`
B) `npm add express`
C) `node install express`
D) `npm get express`

---

### Question 2
Quelle ligne doit être ajoutée dans `package.json` pour utiliser la syntaxe ESM (import/export) ?

A) `"module": "es6"`
B) `"type": "module"`
C) `"esm": true`
D) `"import": "enabled"`

---

### Question 3
Comment importer Express en utilisant la syntaxe ESM ?

A) `const express = require('express');`
B) `import express from 'express';`
C) `import { express } from 'express';`
D) `require('express');`

---

### Question 4
Quelle méthode HTTP est utilisée pour récupérer des données ?

A) POST
B) GET
C) PUT
D) DELETE

---

### Question 5
Comment créer une route GET sur `/about` qui renvoie du texte ?

A) `app.get('/about', (req, res) => { res.send('About'); });`
B) `app.route('/about').get('About');`
C) `app.createRoute('GET', '/about', 'About');`
D) `app.get('About', '/about');`

---

### Question 6
Comment accéder au paramètre `id` dans l'URL `/users/:id` ?

A) `req.id`
B) `req.params.id`
C) `req.query.id`
D) `req.body.id`

---

### Question 7
Pour l'URL `/search?q=express&page=2`, comment accéder à la valeur de `page` ?

A) `req.params.page`
B) `req.query.page`
C) `req.body.page`
D) `req.url.page`

---