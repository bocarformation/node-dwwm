---

## Questions

### Question 1
Quelle est la principale différence entre GET et POST ?

A) GET est plus rapide que POST
B) GET récupère des données, POST envoie des données
C) GET fonctionne avec JSON, POST avec HTML
D) GET nécessite express.json(), POST non

---

### Question 2
Quel middleware Express est OBLIGATOIRE pour lire les données JSON envoyées en POST ?

A) `app.use(express.static())`
B) `app.use(express.json())`
C) `app.use(express.urlencoded())`
D) `app.use(express.text())`

---

### Question 3
Dans une route POST, où se trouvent les données envoyées par le client ?

A) `req.params`
B) `req.query`
C) `req.body`
D) `req.headers`

---

### Question 4
Quel code HTTP doit-on renvoyer lors de la création réussie d'une ressource ?

A) 200
B) 201
C) 204
D) 400

---

### Question 5
Que fait cette ligne de code : `const id = parseInt(req.params.id);` ?

A) Récupère l'ID depuis le body de la requête
B) Récupère l'ID depuis l'URL et le convertit en nombre
C) Récupère l'ID depuis les headers
D) Crée un nouvel ID automatiquement

---

### Question 6
Quelle méthode JavaScript permet d'ajouter un élément à la fin d'un tableau ?

A) `add()`
B) `push()`
C) `append()`
D) `insert()`

---

### Question 7
Quelle méthode JavaScript permet de trouver un élément dans un tableau ?

A) `search()`
B) `find()`
C) `filter()`
D) `locate()`

---

### Question 8
Quel code HTTP renvoie-t-on quand une ressource n'est pas trouvée ?

A) 400
B) 401
C) 404
D) 500

---

### Question 9
Quelle méthode JavaScript permet de trouver l'index (position) d'un élément dans un tableau ?

A) `findIndex()`
B) `indexOf()`
C) `searchIndex()`
D) `getIndex()`


---

### Question 10
Quel code HTTP renvoie-t-on quand les données envoyées sont invalides (mauvaise requête) ?

A) 200
B) 400
C) 404
D) 500

---